In order to run the python script to collect restaurants from yelp API. You should first install the required library. 

Run the following command in your terminal:
1. pip install requests_aws4auth
2. pip install opensearch-py

Then replace the credentials (e.g. password, AWS access key id and secret key) with your own credentials in main function.

Then you are good to run the script.  

